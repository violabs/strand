server:
  port: 8081
  tomcat:
    threads:
      max: 10

spring:
  main:
    web-application-type: servlet

logging:
  level:
    io.violabs.webclient.client: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,threaddump